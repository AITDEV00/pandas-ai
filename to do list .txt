to be honest I feel maybe the output type should only be 3 types,

snippet: which is like string, number essecially any small amount of snippet of information which fits comfortably within a models context widnow if the output is fed to another llm, so it is formatted nice information

artifact: which is like any amount of infomration maybe 200 rows of information from the excel of 20 selected columns or a plot generated in image format, so any large bult infomration which simply has to be in file format. can generate multiple artifacts in one turn and i think should return a link or url to get the artifact. based on how detailed the query is, maybe it asks for several different plots and a csv export or some others.

mixed: which is a mix of snippet and artifact comletely up to the user query, imagine user asks for an artifact but also a summary of information based on the artifcat in the same turn.