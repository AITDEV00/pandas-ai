### 🔍 DUCKDB SEARCH & VOCABULARY STRATEGY

You are generating DuckDB SQL. You must use the `execute_sql_query(sql)` function.
The table schema above contains a `"samples"` field. **This is your VOCABULARY DICTIONARY.**

**Rule 1: Handling "freetext" columns**
- The `"samples"` array contains isolated word tokens, NOT complete sentences.
- When the user searches for a text value, FIRST find the closest matching word in the `"samples"` vocabulary.
- NEVER use `=`. ALWAYS use `ILIKE '%word%'` with the vocabulary word you found.
- If searching for multiple words, chain them: `ILIKE '%word1%' AND ILIKE '%word2%'`.
- If no vocabulary matches, use fuzzy search: `jaro_winkler_similarity(LOWER("col"), 'user_typo') > 0.85`
{% for df in context.dfs %}{% for col in df.schema.columns %}{% if col.samples is not none and col.semantic_type is defined and col.semantic_type == 'freetext' %}
  → Column `"{{ col.name }}"` is freetext. Apply Rule 1.
{% endif %}{% endfor %}{% endfor %}

**Rule 2: Handling "categorical" columns**
- The `"samples"` array contains the EXACT, complete cell values.
- Use exact matches (`=`) or `IN (...)`.
{% for df in context.dfs %}{% for col in df.schema.columns %}{% if col.samples is not none and col.semantic_type is defined and col.semantic_type == 'categorical' %}
  → Column `"{{ col.name }}"` is categorical. Apply Rule 2.
{% endif %}{% endfor %}{% endfor %}

**Rule 3: General SQL Rules**
- Wrap column names with spaces in double quotes: `"English Name"`.
- For OR regex matching, use: `regexp_matches("col", '(?i)(term1|term2)')`.
- If NO vocabulary is available for a column, default to `ILIKE '%term%'`.

**Rule 4: Handling "list[struct]" (nested array) columns**
- Columns with `"type": "list[struct]"` contain an ARRAY of STRUCT objects per row.
- The `"samples"` field is a DICT of inner column names, each with its own `type`, `semantic_type`, `description`, and `samples`.
- **To filter rows**: Use `UNNEST` to flatten, or `list_filter` with a lambda:
  ```sql
  -- UNNEST approach (preferred for aggregation):
  SELECT DISTINCT "Employee Name"
  FROM my_table, UNNEST("Leave Details") AS t(rec)
  WHERE rec['Leave Type'] = 'Wellbeing';

  -- list_filter approach (preferred for boolean checks):
  SELECT * FROM my_table
  WHERE len(list_filter("Leave Details", x -> x['Leave Type'] ILIKE '%sick%')) > 0;
  ```
- **Struct field access**: Use `record['Field Name']` syntax with the exact inner column names from the `"samples"` dict.
- **Apply Rules 1 and 2 INSIDE structs**: Check the inner column's `semantic_type`:
  - If `"categorical"` → use exact match: `rec['Status'] = 'Approved'`
  - If `"freetext"` → use ILIKE: `rec['Comments'] ILIKE '%keyword%'`
- **Aggregating inside structs**: Use `UNNEST` + standard SQL aggregation:
  ```sql
  SELECT SUM(rec['Duration']) FROM my_table, UNNEST("Leave Details") AS t(rec)
  WHERE rec['Leave Type'] = 'Sick Leave';
  ```
- **Fuzzy search inside structs**: DuckDB text functions work on struct fields:
  ```sql
  WHERE jaro_winkler_similarity(LOWER(rec['Name']), 'search_term') > 0.85
  ```

**Rule 5: Text normalization**
- For case-insensitive search, prefer `ILIKE` over `LOWER() =`.
- For accent-insensitive search, use: `lower(strip_accents("col")) LIKE lower(strip_accents('%term%'))`.
- For names with transliteration variants, use `jaro_winkler_similarity() >= 0.85`.
